# 定义项目平台和版本
platform :ios, '11.0' # 建议使用一个较新的版本，如 11.0 或更高

# 主应用 Target
target 'ShoppingCartCucumberish' do
  # 这里可以放主应用的 Pods
end

# 单元测试 Target
target 'ShoppingCartCucumberishTests' do
  inherit! :search_paths
  # 这里可以放单元测试的 Pods
end

# UI 测试 Target
target 'ShoppingCartCucumberishUITests' do
  # 将 Cucumberish 添加到 UI 测试 Target
end
